# Copy to .env.production before building for production.
# API Gateway invoke URL (no trailing slash) — set after Phase 4 step 4.

VITE_API_URL=https://YOUR_API_ID.execute-api.us-west-2.amazonaws.com
